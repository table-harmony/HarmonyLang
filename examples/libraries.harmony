import math from "math"
import time from "time"
import random from "random"
import os from "os"

printf(math.abs(-10))

const float = random.float()
println(float)

os.write_file("src/readme.txt", "liron kaner")

const arr = [1, 2, 3]

const shuffle = random.shuffle(arr)
println(shuffle)

const choice = random.choice(arr)
println(choice)

time.sleep(1000)
println(time.now())