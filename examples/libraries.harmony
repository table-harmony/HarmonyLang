import { abs as cool_abs }, math from "math"
import time from "time"
import random from "random"
import os from "os"

printf(cool_abs(-10))
printf(math.pow(2, 3))

const float = random.float()
println(float)

os.write_file("readme.txt", "liron kaner")

const arr = [1, 2, 3]

const shuffle = random.shuffle(arr)
println(shuffle)

const choice = random.choice(arr)
println(choice)

time.sleep(1000)
println(time.now())
