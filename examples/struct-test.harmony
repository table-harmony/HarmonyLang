struct Rectangle {
  static counter = 129

  width: number
  height: number

  fn getSize() -> number {
    return self.width * self.height
  }

  static fn getCounter() -> number {
    return 1123
  }
}

//const square = Rectangle{10, 20,}

//printf(Rectangle.getCounter())
printf(Rectangle)
Rectangle.counter = 1231
printf(Rectangle.counter)
printf(Rectangle.getCounter())
Rectangle.counter = 123
